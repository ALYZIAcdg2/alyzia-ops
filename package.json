{
  "name": "alyzia-ops-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.js && for file in src/v3/*.js; do node --check \"$file\" || exit 1; done"
  },
  "devDependencies": {
    "wrangler": "4.129.0"
  }
}
